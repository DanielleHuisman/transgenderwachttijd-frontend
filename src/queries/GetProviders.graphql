# import '../fragments/Location.graphql'
# import '../fragments/Provider.graphql'

query GetProviders {
    providers {
        ...ProviderFragment

        locations {
            ...LocationFragment
        }
    }
}
